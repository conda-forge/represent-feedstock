{% set name = "Represent" %}
{% set version = "2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 352ec74c11e49959867ad3ceedb029376d815b199bf588458c1298815378012b

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python
    - pip
    - six >=1.8.0
  run:
    - python
    - six >=1.8.0

test:
  imports:
    - represent

about:
  home: https://github.com/RazerM/represent
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Create __repr__ automatically or declaratively.'

  description: |
    Create __repr__ automatically or declaratively.
  doc_url: https://github.com/RazerM/represent
  dev_url: https://github.com/RazerM/represent

extra:
  recipe-maintainers:
    - simonvh
